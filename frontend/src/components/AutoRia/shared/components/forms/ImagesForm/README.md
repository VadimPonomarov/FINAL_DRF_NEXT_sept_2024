# ImagesForm Component

Модульный компонент для управления изображениями объявлений с поддержкой загрузки, генерации и редактирования.

## 📁 Структура

```
ImagesForm/
├── index.tsx              # Главный экспорт
├── ImagesForm.tsx         # Основной компонент (1303 строк)
├── types.ts              # TypeScript типы
├── README.md             # Документация
└── [Pending]
    ├── hooks/            # Кастомные хуки
    ├── components/       # Подкомпоненты
    └── utils/            # Утилиты
```

## 🎯 Функциональность

- ✅ **Загрузка изображений** - Drag & Drop, выбор файлов
- ✅ **Генерация AI** - Создание изображений через API
- ✅ **Управление** - Удаление, переупорядочение, установка главного
- ✅ **Существующие изображения** - Редактирование при обновлении объявления
- ✅ **Галерея** - Просмотр с миниатюрами

## 📦 Использование

```typescript
import { ImagesForm } from '@/components/AutoRia/shared/components/forms/ImagesForm';

<ImagesForm
  data={formData}
  onChange={handleFormChange}
  errors={errors}
  adId={adId}
/>
```

## 🔄 Миграция

### Было
```typescript
import ImagesForm from '@/components/AutoRia/Forms/ImagesForm';
```

### Стало
```typescript
import { ImagesForm } from '@/components/AutoRia/shared/components/forms/ImagesForm';
// или
import ImagesForm from '@/components/AutoRia/Forms/ImagesForm'; // переэкспорт
```

## 📊 Статистика

- **Оригинальный размер**: 57.89 KB (1303 строк)
- **Статус**: 🔄 В процессе модуляризации
- **Следующий шаг**: Извлечение хуков и компонентов

## 🎓 Типы

```typescript
interface ImagesFormProps {
  data: Partial<CarAdFormData>;
  onChange: (data: Partial<CarAdFormData>) => void;
  errors?: string[];
  adId?: number;
}

interface ImageGenerationOptions {
  imageTypes: string[];
  mode: 'add' | 'replace' | 'update';
  replaceExisting: boolean;
  onlyMissing: boolean;
  replaceEmpty: boolean;
}
```

## 🚀 Планы

- [ ] Извлечение `useImageUpload` хука
- [ ] Извлечение `useImageGeneration` хука
- [ ] Извлечение `ImageUploadTab` компонента
- [ ] Извлечение `ImageGenerationTab` компонента
- [ ] Извлечение утилит валидации
- [ ] Оптимизация производительности

---

**Статус**: 🔄 Модуляризация в процессе  
**Дата**: November 27, 2025
