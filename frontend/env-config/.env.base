# Base (committed) defaults for local/dev
# These values are safe for local development and allow running the app out of the box

# Frontend toggles
NEXT_PUBLIC_IS_DOCKER=false

# Backend base URL (Django API)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Redis config (used in app features / mocks)
REDIS_HOST=localhost
REDIS_URL=redis://localhost:6379/0

# General app
NODE_ENV=development
