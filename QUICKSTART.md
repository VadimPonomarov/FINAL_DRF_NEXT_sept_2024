# ⚡ Швидкий старт — AutoRia Clone

Це короткий гайд для тих, хто хоче розгорнути проєкт за 5 хвилин.

## 📋 Що потрібно встановити

1. **Docker Desktop** — https://www.docker.com/products/docker-desktop/
2. **Git** — https://git-scm.com/downloads

## 🚀 Три прості кроки

### Крок 1: Клонуйте репозиторій

Відкрийте термінал і виконайте:

```bash
git clone <URL_РЕПОЗИТОРІЮ> autoria-clone
cd autoria-clone
```

### Крок 2: Запустіть автоматичне розгортання

Обирайте зручний варіант:

**Windows:**
```bash
python deploy.py
```

**Linux/Mac:**
```bash
chmod +x deploy.sh
./deploy.sh
```

**Якщо встановлено Node.js:**
```bash
node deploy.js
```

### Крок 3: Відкрийте браузер

Зачекайте 2–3 хвилини і перейдіть за адресою:

👉 **http://localhost:3000**

## 🔐 Вхід до системи

- **Email**: `admin@autoria.com`
- **Password**: `12345678`

## ✅ Що перевірити

Після входу переконайтеся, що бачите:
- ✅ щонайменше 10 **активних** тестових оголошень з реальними (не плейсхолдерними) фотографіями
- ✅ Працюючий пошук
- ✅ Фільтри за марками та моделями
  
Під час сидингу всі тестові оголошення, для яких не вдалося згенерувати жодної реальної фотографії, автоматично видаляються. У публічних списках залишаються тільки оголошення з валідними фото.

## 🐛 Якщо щось пішло не так

### Проблема: «Docker не запускається»

**Що робити:**
1. Відкрийте Docker Desktop
2. Дочекайтеся зеленого статусу «Engine running»
3. Запустіть скрипт розгортання ще раз

### Проблема: «Порт 3000 зайнятий»

**Що робити:**
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:3000 | xargs kill -9
```

### Проблема: «Something went wrong» або «invariant expected app router to be mounted»

**Причина:** Відсутній `.env.local` або старий `.next` зібраний без правильних змінних.

**Що робити:**
```bash
# 1. Створити .env.local
python scripts/setup-frontend-env.py

# 2. ОБОВ'ЯЗКОВО очистити стару збірку
cd frontend
rm -rf .next node_modules/.cache  # Linux/Mac
# або
Remove-Item -Path .next, node_modules\.cache -Recurse -Force  # Windows

# 3. Пересобрати
npm run build

# 4. Запустити
npm run start
```

### Проблема: «Немає тестових оголошень»

**Що робити:**
1. Увійдіть у систему
2. Натисніть «🎲 Demo Ads Generator» на головній сторінці
3. Оберіть 10 оголошень
4. Натисніть «Створити»

## 🐳 Запуск через Docker Compose (без скриптів)

Якщо не хочете використовувати `deploy.py`, `deploy.sh` або `deploy.js`, можна запустити все напряму через Docker Compose.

### Варіант A: Full Docker (backend + frontend в контейнерах)

```bash
docker compose -f docker-compose.yml -f docker-compose.with_frontend.yml up -d --build
```

або старий синтаксис:

```bash
docker-compose -f docker-compose.yml -f docker-compose.with_frontend.yml up -d --build
```

Щоб зупинити full‑Docker варіант:

```bash
docker compose -f docker-compose.yml -f docker-compose.with_frontend.yml down
```

### Варіант B: Backend в Docker, frontend локально

```bash
docker compose -f docker-compose.yml up -d --build

cd frontend
npm install
npm run build
npm run start
```

## 📚 Детальна документація

Шукаєте більше деталей? Перейдіть до [DEPLOYMENT.md](./DEPLOYMENT.md)

## 🛑 Зупинка проєкту

```bash
docker-compose down
```

## 🔄 Повторний запуск

```bash
docker-compose up -d
```

---

**Готово! Насолоджуйтеся AutoRia Clone! 🎉**
