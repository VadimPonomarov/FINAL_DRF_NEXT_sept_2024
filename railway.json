{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'python manage.py migrate --noinput; python manage.py collectstatic --noinput; gunicorn config.wsgi_simple:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --access-logfile -'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 4,
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 300
  }
}
