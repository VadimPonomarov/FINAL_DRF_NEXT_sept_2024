# 🚀 AutoRia Clone — Повний посібник з розгортання

Цей документ допоможе вам розгорнути повноцінний клон AutoRia з нуля.

## 📋 Зміст

1. [Системні вимоги](#системні-вимоги)
2. [Клонування репозиторію](#клонування-репозиторію)
3. [Автоматичне розгортання](#автоматичне-розгортання)
4. [Ручне розгортання](#ручне-розгортання)
5. [Перевірка працездатності](#перевірка-працездатності)
6. [Усунення несправностей](#усунення-несправностей)
7. [Корисні команди](#корисні-команди)
8. [Наступні кроки](#наступні-кроки)
9. [Підтримка](#підтримка)
10. [Ліцензія](#ліцензія)

---

## 🖥️ Системні вимоги

### Мінімально необхідне ПЗ

- **Docker** версії 20.10 або новішої
- **Docker Compose** версії 2.0 або новішої
- **Git** для клонування репозиторію

### Додатково (для локального запуску фронтенду)

- **Node.js** версії 18.x або новішої
- **npm** версії 9.x або новішої

### Перевірка наявних версій

```bash
docker --version
docker-compose --version
git --version
node --version    # якщо плануєте запускати frontend локально
npm --version     # якщо плануєте запускати frontend локально
```

---

## 📥 Клонування репозиторію

### Крок 1. Клонувати проєкт

```bash
git clone <URL_ВАШОГО_РЕПОЗИТОРІЮ> autoria-clone
cd autoria-clone
```

### Крок 2. Перевірити структуру

```bash
ls -la
```

Очікувана структура:
- `backend/` — Django backend
- `frontend/` — Next.js frontend
- `docker-compose.yml` — конфігурація Docker
- `deploy.py` — скрипт розгортання (Python)
- `deploy.sh` — скрипт розгортання (Bash)
- `deploy.js` — скрипт розгортання (Node.js)
- `scripts/check_environment.py` — перевірка середовища (Python)

### Крок 3. Швидка перевірка середовища (опційно)

Python-скрипт `deploy.py` **автоматично** виконує повну перевірку середовища
(`scripts/check_environment.py`) перед будь-яким деплоєм і зупиняє процес,
якщо є критичні помилки (Docker Engine не запущений, немає Node.js тощо).

Якщо ви запускаєте тільки `docker-compose` вручну або хочете окремо
подивитися детальну діагностику, можна виконати:

```bash
python scripts/check_environment.py
```

Якщо якась перевірка не пройдена (наприклад, Docker Engine не
запущений), виправте це перед розгортанням.

---

## 🤖 Автоматичне розгортання

Оберіть зручний для себе спосіб.

### Вариант 1. Python-скрипт (рекомендовано)

```bash
# 1) Backend у Docker, frontend локально (найпростіший спосіб)
python deploy.py --mode local

# 2) Повне розгортання в Docker (backend + frontend у контейнерах)
python deploy.py --mode with_frontend

# 3) Інтерактивний вибір режиму (за запитом у консолі)
python deploy.py
```

> ℹ️ **Повний Docker запуск** (`--mode with_frontend`) автоматично передає обидва файли `docker-compose.yml` та `docker-compose.with_frontend.yml` у `docker-compose`. Якщо ви запускаєте вручну, використовуйте `docker-compose -f docker-compose.yml -f docker-compose.with_frontend.yml up -d --build` — це вмикає сервіс Frontend.

> 💡 За потреби перед запуском можна додатково звільнити порти
> за допомогою утиліти:
>
> ```bash
> python scripts/cleanup_ports.py --ports 80 3000 5432 5555 5540 6379 8000 8001 15672
```

### Варіант 2. Bash-скрипт (Linux/Mac)

```bash
# Надати права на виконання
chmod +x deploy.sh

# Повне розгортання в Docker
./deploy.sh

# Або з локальним фронтендом
./deploy.sh --local-frontend
```

### Варіант 3. Node.js-скрипт

```bash
# Повне розгортання в Docker
node deploy.js

# Або з локальним фронтендом
node deploy.js --local-frontend
```

### Що роблять автоматичні скрипти

1. ✅ Перевіряють наявність необхідних інструментів
2. ✅ Зупиняють активні контейнери (за потреби)
3. ✅ Піднімають Docker-сервіси
4. ✅ Чекають готовності бекенду та фронтенду
5. ✅ Встановлюють залежності (для локального фронтенду)
6. ✅ Будують та запускають застосунок
7. ✅ Виводять корисні посилання й інструкції

### Час розгортання

- **Вперше**: 5–10 хвилин (збірка образів)
- **Наступні рази**: 1–2 хвилини

---

## 🔧 Ручне розгортання

Якщо автоматизація недоступна, скористайтесь ручними інструкціями.

### Варіант A. Повний запуск у Docker

#### Крок 1. Запустити контейнери

```bash
docker-compose -f docker-compose.yml -f docker-compose.with_frontend.yml up -d --build
```

#### Крок 2. Дочекатися готовності сервісів

```bash
# Перевірка статусу
docker-compose -f docker-compose.yml -f docker-compose.with_frontend.yml ps

# Перегляд логів бекенду
docker-compose -f docker-compose.yml -f docker-compose.with_frontend.yml logs -f app
```

Очікуйте повідомлення `Listening on TCP address 0.0.0.0:8000`.

#### Крок 3. Перевірити доступ

- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- Swagger-документація: http://localhost:8000/api/docs/

### Варіант B. Бекенд у Docker, фронтенд локально

#### Крок 1. Створити `.env.local` для frontend

**⚠️ КРИТИЧНО ВАЖЛИВО!** Next.js потребує файл `.env.local` з `NEXTAUTH_SECRET`.

**Автоматично (рекомендовано):**
```bash
python scripts/setup-frontend-env.py
```

**Вручну:**
```bash
# Створити файл frontend/.env.local
cat > frontend/.env.local << 'EOF'
# Критичні змінні для Next.js
NEXTAUTH_SECRET=bXL+w0/zn9FX477unDrwiDMw8Tn4uC2Jv5fK3pL9mN6qR8sT1vW4xY7zA0bC
NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
BACKEND_URL=http://localhost:8000
NODE_ENV=production
NEXT_PUBLIC_IS_DOCKER=false
IS_DOCKER=false
REDIS_HOST=localhost
REDIS_URL=redis://localhost:6379/0
EOF
```

> 💡 **Примітка**: Без `NEXTAUTH_SECRET` frontend покаже помилку "Something went wrong"!

#### Крок 2. Запустити бекенд-сервіси

```bash
docker-compose up -d --build
```

#### Крок 3. Перевірити здоровʼя бекенду

```bash
# Дочекатися поки контейнери стануть healthy
docker-compose ps

# Перевірити health endpoint
curl http://localhost:8000/health
```

#### Крок 4. Очистити попередні артефакти збірки

**⚠️ КРИТИЧНО ВАЖЛИВО!** Завжди очищайте `.next` перед новою збіркою.

```bash
cd frontend

# Видалити попередню збірку
Remove-Item -Path .next -Recurse -Force -ErrorAction SilentlyContinue  # Windows PowerShell
# або
rm -rf .next  # Linux/Mac

# Видалити кеш
Remove-Item -Path node_modules\.cache -Recurse -Force -ErrorAction SilentlyContinue  # Windows
# або
rm -rf node_modules/.cache  # Linux/Mac
```

#### Крок 5. Встановити залежності фронтенду

```bash
npm install --legacy-peer-deps
```

#### Крок 6. Зібрати та запустити фронтенд

```bash
# Збірка
npm run build

# Запуск
npm run start
```

#### Крок 6. Перевірити в браузері

- Frontend: http://localhost:3000
- Backend API: http://localhost:8000/api/

---

## ✅ Перевірка працездатності

### 1. Перевірити сервіси

```bash
# Бекенд
curl http://localhost:8000/health

# Фронтенд
curl http://localhost:3000
```

### 2. Увійти в систему

- Перейдіть на http://localhost:3000
- Авторизуйтеся:
  - **Email**: `admin@autoria.com`
  - **Password**: `12345678`

### 3. Переконатися в наявності тестових оголошень
  
  - Перейдіть на http://localhost:3000
  - Авторизуйтеся:
    - **Email**: `admin@autoria.com`
    - **Password**: `12345678`
 
 Після розгортання й першого входу ви повинні бачити щонайменше 10 **активних** тестових оголошень з реальними (не плейсхолдерними) фотографіями. Тестові оголошення, для яких не вдалося згенерувати жодної реальної фотографії, автоматично видаляються під час сидингу.
 
  ### 4. Перевірити API-документацію

Відкрийте http://localhost:8000/api/docs/ (Swagger UI).

---

## 🐛 Усунення несправностей

### Docker-контейнери не стартують

```bash
# Зупинити контейнери
docker-compose down

# Видалити томи (увага: дані втратяться)
docker-compose down -v

# Побудувати образи заново
docker-compose build --no-cache

# Запустити
docker-compose up -d
```

### Бекенд не відповідає

```bash
# Подивитися логи
docker-compose logs app

# Перезапустити бекенд
docker-compose restart app

# Переконатися, що PostgreSQL працює
docker-compose ps pg
```

### Помилка "Something went wrong" на frontend

**Причина:** Відсутній `NEXTAUTH_SECRET` у `frontend/.env.local`

**Рішення:**
```bash
# Автоматично створити .env.local
python scripts/setup-frontend-env.py

# Або додати вручну в frontend/.env.local:
echo "NEXTAUTH_SECRET=bXL+w0/zn9FX477unDrwiDMw8Tn4uC2Jv5fK3pL9mN6qR8sT1vW4xY7zA0bC" >> frontend/.env.local

# Перезапустити frontend
cd frontend
npm run start
```

### Фронтенд не бачить бекенд

1. Перевірте що `frontend/.env.local` існує та містить:
   ```
   NEXTAUTH_SECRET=bXL+w0/zn9FX477unDrwiDMw8Tn4uC2Jv5fK3pL9mN6qR8sT1vW4xY7zA0bC
   NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
   ```
2. Переконайтеся, що бекенд живий:
   ```bash
   curl http://localhost:8000/health
   ```
3. Перегляньте налаштування CORS у `backend/core/settings.py`

### Не створилися тестові оголошення

1. Увійдіть як admin
2. На головній сторінці натисніть «🎲 Demo Ads Generator»
3. Оберіть кількість оголошень і типи фото
4. Натисніть «Створити»

Або скористайтеся API:

```bash
curl -X POST http://localhost:8000/api/autoria/test-ads/generate \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "count": 10,
    "includeImages": true,
    "imageTypes": ["front", "side"]
  }'
```

### Порт 3000 або 8000 зайнятий

1. Завершіть процеси:
   ```bash
   # Windows
   netstat -ano | findstr :3000
   taskkill /PID <PID> /F

   # Linux/Mac
   lsof -ti:3000 | xargs kill -9
   ```
2. Або змініть порти в `docker-compose.yml`:
   ```yaml
   ports:
     - "3001:3000"
     - "8001:8000"
   ```

### Docker не вистачає памʼяті

- **Docker Desktop**: Settings → Resources → Memory (рекомендуємо ≥ 4 GB)
- **Linux**: відредагуйте `/etc/docker/daemon.json`

---

## 📊 Корисні команди

### Контейнери

```bash
# Логи всіх сервісів
docker-compose logs -f

# Логи конкретного сервісу
docker-compose logs -f app
docker-compose logs -f frontend

# Перезапустити сервіс
docker-compose restart app

# Зупинити всі сервіси
docker-compose down

# Зупинити та видалити томи
docker-compose down -v
```

### База даних

```bash
# Підключитися до PostgreSQL
docker-compose exec pg psql -U postgres -d autoria_db

# Зробити бекап
docker-compose exec pg pg_dump -U postgres autoria_db > backup.sql

# Відновити з бекапу
docker-compose exec -T pg psql -U postgres autoria_db < backup.sql
```

### Django

```bash
# Будь-яка команда
docker-compose exec app python manage.py <command>

# Створити суперкористувача
docker-compose exec app python manage.py createsuperuser

# Міграції
docker-compose exec app python manage.py migrate

# Збірка статичних файлів
docker-compose exec app python manage.py collectstatic --noinput
```

---

## 🎯 Наступні кроки

Після успішного розгортання рекомендуємо:

1. **Змінити паролі за замовчуванням** в адмін-панелі
2. **Налаштувати змінні середовища** для продакшену
3. **Увімкнути HTTPS** (SSL-сертифікат)
4. **Налаштувати резервне копіювання** бази даних
5. **Додати моніторинг** і централізовані логи

---

## 📞 Підтримка

Якщо виникли питання:

1. Перевірте розділ [Усунення несправностей](#усунення-несправностей)
2. Перегляньте логи: `docker-compose logs -f`
3. Створіть issue у репозиторії з описом ситуації

---

## 📝 Ліцензія

Проєкт розповсюджується за ліцензією MIT.
