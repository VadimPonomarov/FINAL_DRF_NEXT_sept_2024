# 🚀 AutoRia Clone - Полное руководство по развертыванию

Это руководство поможет вам развернуть полнофункциональный клон AutoRia с нуля.

## 📋 Содержание

1. [Системные требования](#системные-требования)
2. [Клонирование репозитория](#клонирование-репозитория)
3. [Автоматическое развертывание](#автоматическое-развертывание)
4. [Ручное развертывание](#ручное-развертывание)
5. [Проверка работоспособности](#проверка-работоспособности)
6. [Решение проблем](#решение-проблем)

---

## 🖥️ Системные требования

### Минимальные требования:

- **Docker**: версия 20.10 или выше
- **Docker Compose**: версия 2.0 или выше
- **Git**: для клонирования репозитория

### Дополнительно (для локального запуска frontend):

- **Node.js**: версия 18.x или выше
- **npm**: версия 9.x или выше

### Проверка установленных версий:

```bash
docker --version
docker-compose --version
git --version
node --version  # если планируете запускать frontend локально
npm --version   # если планируете запускать frontend локально
```

---

## 📥 Клонирование репозитория

### Шаг 1: Клонируйте репозиторий

```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> autoria-clone
cd autoria-clone
```

### Шаг 2: Проверьте структуру проекта

```bash
ls -la
```

Вы должны увидеть следующие директории и файлы:
- `backend/` - Django backend
- `frontend/` - Next.js frontend
- `docker-compose.yml` - конфигурация Docker
- `deploy.py` - скрипт развертывания (Python)
- `deploy.sh` - скрипт развертывания (Bash)
- `deploy.js` - скрипт развертывания (Node.js)

---

## 🤖 Автоматическое развертывание

Выберите один из трех способов автоматического развертывания:

### Вариант 1: Python скрипт (рекомендуется)

```bash
# Полное развертывание в Docker
python deploy.py

# Или с локальным frontend
python deploy.py --local-frontend
```

### Вариант 2: Bash скрипт (Linux/Mac)

```bash
# Сделайте скрипт исполняемым
chmod +x deploy.sh

# Полное развертывание в Docker
./deploy.sh

# Или с локальным frontend
./deploy.sh --local-frontend
```

### Вариант 3: Node.js скрипт

```bash
# Полное развертывание в Docker
node deploy.js

# Или с локальным frontend
node deploy.js --local-frontend
```

### Что делают скрипты:

1. ✅ Проверяют системные требования
2. ✅ Останавливают существующие контейнеры
3. ✅ Запускают Docker контейнеры
4. ✅ Ожидают готовности всех сервисов
5. ✅ Устанавливают зависимости (если frontend локально)
6. ✅ Собирают и запускают приложение
7. ✅ Выводят информацию о доступе

### Время развертывания:

- **Первый запуск**: 5-10 минут (сборка образов)
- **Последующие запуски**: 1-2 минуты

---

## 🔧 Ручное развертывание

Если автоматические скрипты не работают, следуйте этим шагам:

### Вариант A: Полное развертывание в Docker

#### Шаг 1: Запустите Docker контейнеры

```bash
docker-compose up -d --build
```

#### Шаг 2: Дождитесь готовности сервисов

```bash
# Проверьте статус контейнеров
docker-compose ps

# Проверьте логи
docker-compose logs -f app
```

Дождитесь сообщения: `Listening on TCP address 0.0.0.0:8000`

#### Шаг 3: Проверьте доступность

- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/api/docs/

### Вариант B: Docker backend + Локальный frontend

#### Шаг 1: Запустите только backend сервисы

```bash
docker-compose up -d pg redis rabbitmq redis-insight mailing celery-worker celery-beat celery-flower app
```

#### Шаг 2: Дождитесь готовности backend

```bash
# Проверьте health endpoint
curl http://localhost:8000/health
```

#### Шаг 3: Установите зависимости frontend

```bash
cd frontend
npm install
```

#### Шаг 4: Создайте .env.local файл

```bash
# frontend/.env.local
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000
```

#### Шаг 5: Соберите и запустите frontend

```bash
# Сборка
npm run build

# Запуск
npm run start
```

#### Шаг 6: Откройте в браузере

- Frontend: http://localhost:3000

---

## ✅ Проверка работоспособности

### 1. Проверьте доступность сервисов

```bash
# Backend health check
curl http://localhost:8000/health

# Frontend
curl http://localhost:3000
```

### 2. Войдите в систему

Откройте http://localhost:3000 и войдите с учетными данными:

- **Email**: `admin@autoria.com`
- **Password**: `12345678`

### 3. Проверьте тестовые объявления

После первого входа система автоматически создаст 10 тестовых объявлений с изображениями.

Перейдите на главную страницу и убедитесь, что объявления отображаются.

### 4. Проверьте API документацию

Откройте http://localhost:8000/api/docs/ для просмотра Swagger документации API.

---

## 🐛 Решение проблем

### Проблема: Docker контейнеры не запускаются

**Решение:**

```bash
# Остановите все контейнеры
docker-compose down

# Удалите volumes (ВНИМАНИЕ: удалит все данные)
docker-compose down -v

# Пересоберите образы
docker-compose build --no-cache

# Запустите снова
docker-compose up -d
```

### Проблема: Backend не отвечает

**Решение:**

```bash
# Проверьте логи backend
docker-compose logs app

# Перезапустите backend
docker-compose restart app

# Проверьте, что PostgreSQL запущен
docker-compose ps pg
```

### Проблема: Frontend не подключается к backend

**Решение:**

1. Проверьте переменные окружения в `frontend/.env.local`:
   ```
   NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
   ```

2. Убедитесь, что backend доступен:
   ```bash
   curl http://localhost:8000/health
   ```

3. Проверьте CORS настройки в `backend/core/settings.py`

### Проблема: Нет тестовых объявлений

**Решение:**

1. Войдите в систему как admin
2. Откройте главную страницу
3. Нажмите кнопку "🎲 Demo Ads Generator"
4. Выберите количество объявлений и типы изображений
5. Нажмите "Создать"

Или используйте API напрямую:

```bash
curl -X POST http://localhost:8000/api/autoria/test-ads/generate \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "count": 10,
    "includeImages": true,
    "imageTypes": ["front", "side"]
  }'
```

### Проблема: Порты заняты

**Решение:**

Если порты 3000 или 8000 уже используются:

1. Остановите процессы, использующие эти порты:
   ```bash
   # Windows
   netstat -ano | findstr :3000
   taskkill /PID <PID> /F
   
   # Linux/Mac
   lsof -ti:3000 | xargs kill -9
   ```

2. Или измените порты в `docker-compose.yml`:
   ```yaml
   ports:
     - "3001:3000"  # Изменить frontend порт
     - "8001:8000"  # Изменить backend порт
   ```

### Проблема: Недостаточно памяти Docker

**Решение:**

Увеличьте лимиты памяти Docker:

- **Docker Desktop**: Settings → Resources → Memory (минимум 4GB)
- **Linux**: Настройте `/etc/docker/daemon.json`

---

## 📊 Полезные команды

### Управление контейнерами

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f app
docker-compose logs -f frontend

# Перезапуск сервиса
docker-compose restart app

# Остановка всех сервисов
docker-compose down

# Остановка с удалением volumes
docker-compose down -v
```

### Работа с базой данных

```bash
# Подключение к PostgreSQL
docker-compose exec pg psql -U postgres -d autoria_db

# Создание бэкапа
docker-compose exec pg pg_dump -U postgres autoria_db > backup.sql

# Восстановление из бэкапа
docker-compose exec -T pg psql -U postgres autoria_db < backup.sql
```

### Работа с Django

```bash
# Выполнение команд Django
docker-compose exec app python manage.py <command>

# Создание суперпользователя
docker-compose exec app python manage.py createsuperuser

# Миграции
docker-compose exec app python manage.py migrate

# Сбор статики
docker-compose exec app python manage.py collectstatic --noinput
```

---

## 🎯 Следующие шаги

После успешного развертывания:

1. **Измените пароли по умолчанию** в админ-панели
2. **Настройте переменные окружения** для production
3. **Настройте SSL сертификаты** для HTTPS
4. **Настройте резервное копирование** базы данных
5. **Настройте мониторинг** и логирование

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел [Решение проблем](#решение-проблем)
2. Просмотрите логи: `docker-compose logs -f`
3. Создайте issue в репозитории с описанием проблемы

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.
