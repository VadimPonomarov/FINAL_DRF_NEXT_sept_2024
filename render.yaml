services:
  # Django DRF Backend
  - type: web
    name: autoria-backend
    runtime: python
    region: frankfurt
    plan: free
    branch: master
    rootDir: backend
    buildCommand: pip install poetry==1.8.3 && poetry config virtualenvs.create false && poetry install --no-interaction --no-ansi --only main
    startCommand: python manage.py migrate --noinput && python manage.py init_project_data --force && python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: DEBUG
        value: False
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings
      - key: DJANGO_ALLOWED_HOSTS
        value: .onrender.com,autoria-clone.vercel.app
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: autoria-db
          property: connectionString
      - key: REDIS_URL
        fromDatabase:
          name: autoria-redis
          property: connectionString
      - key: CORS_ALLOWED_ORIGINS
        value: https://autoria-clone.vercel.app
      - key: CORS_ALLOW_CREDENTIALS
        value: True
      - key: IS_DOCKER
        value: false
      - key: IS_SEEDING_REQUIRED
        value: true
    healthCheckPath: /health/

  # Celery Worker (optional)
  - type: worker
    name: autoria-celery-worker
    runtime: python
    region: frankfurt
    plan: free
    branch: master
    rootDir: celery-service
    buildCommand: pip install -r ../backend/requirements.txt
    startCommand: celery -A config worker --loglevel=info
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: autoria-db
          property: connectionString
      - key: REDIS_URL
        fromDatabase:
          name: autoria-redis
          property: connectionString

databases:
  # PostgreSQL Database
  - name: autoria-db
    databaseName: autoria
    user: autoria_user
    region: frankfurt
    plan: free

  # Redis Cache
  - name: autoria-redis
    region: frankfurt
    plan: free
