# 🌐 Професійна Система Перекладів

## ✅ Актуальний стан

Станом на 2025 рік у проекті використовується така реальна конфігурація перекладів:

- **Основний runtime-шар**: `next-intl` та власний контекст `useI18n` з файлів `frontend/src/locales/{en,ru,uk}.ts`.
- **Три основні мови**: `en`, `uk`, `ru` з plain-об'єктами перекладів.
- **Жодні npm-скрипти виду `translations:*` зараз не налаштовані в `frontend/package.json` – більшість Node-скриптів з папки `scripts/` (scan-ui, coverage тощо) були видалені разом з іншими девелоперськими утилітами, щоб залишити тільки скрипти для деплою.

Рекомендований підхід на поточний момент:

- Додавати та змінювати ключі безпосередньо у `frontend/src/locales/{en,ru,uk}.ts`.
- Дотримуватися єдиної ієрархії ключів (наприклад, `autoria.testAds.*`, `common.*`, `navigation.*`).
- По можливості уникати hardcoded текстів у компонентах, підтягаючи строки через `t('...')`/`useI18n()`.

Нижче в цьому документі описано **розширену/планову** систему перекладів на базі `translations.json`, автогенерованих TS-типів та CLI-скриптів аналізу. Вона може використовуватися як дорожня карта для подальшого розвитку, але перед запуском конкретних команд завжди перевіряйте їх наявність у `package.json` та у директорії `scripts/`.

## 📋 Огляд (архівна / планова система)

Повнофункціональна enterprise-рівня система перекладів з TypeScript типізацією, автоматичною валідацією та інструментами для забезпечення якості.

## 🎯 Основні можливості

### ✅ Реалізовано

1. **TypeScript Type-Safety**
   - Автоматична генерація типів з JSON
   - Compile-time перевірка ключів
   - IntelliSense підтримка

2. **Єдиний Індексований Файл**
   - `translations.json` - централізоване зберігання
   - Структурована ієрархія ключів
   - 1511 унікальних ключів (0 дублікатів)

3. **Автоматична Валідація**
   - Перевірка синхронізації між мовами
   - Виявлення дублікатів
   - Контроль покриття (61.48%)

4. **Інструменти Аналізу**
   - Сканер hardcoded текстів
   - Аналіз покриття перекладами
   - Детальні звіти

## 📁 Структура Файлів

```
frontend/src/locales/
├── translations.json          # Головний файл перекладів (280KB)
├── types.ts                   # Auto-generated TypeScript типи
├── en.ts                      # Застарілий - для зворотної сумісності
├── ru.ts                      # Застарілий - для зворотної сумісності
├── uk.ts                      # Застарілий - для зворотної сумісності
├── .migration-backup/         # Backup старих файлів
└── reports/
    ├── translation-coverage-report.json
    ├── hardcoded-texts-report.json
    └── translation-analysis-report.json

scripts/
├── migrate-to-new-translations.js     # Міграція у JSON
├── generate-translation-types.js      # Генерація TS типів
├── validate-translation-coverage.js   # Валідація покриття
├── scan-ui-components.js             # Пошук hardcoded текстів
├── check-translations.js             # Перевірка дублікатів
└── fix-translations.js               # Автовиправлення
```

## 🚀 Команди NPM

### Основні команди

```bash
# Міграція існуючих файлів у новий формат
npm run translations:migrate

# Генерація TypeScript типів
npm run translations:generate-types

# Повна збірка (міграція + типи)
npm run translations:build
```

### Валідація та тестування

```bash
# Перевірка на дублікати
npm run translations:check

# Валідація покриття перекладами
npm run translations:coverage

# Сканування hardcoded текстів
npm run translations:scan-ui

# Повне тестування
npm run translations:test
```

### Виправлення проблем

```bash
# Автоматичне виправлення дублікатів
npm run translations:fix

# Нормалізація та синхронізація
npm run translations:sync
```

## 📊 Поточний Стан

### Статистика (станом на 2025-11-07)

- **Всього ключів**: 1,511
- **Покриття**: 61.48% (929 з 1,511)
- **Дублікати**: 0 (видалено 436 з en.ts)
- **Мови**: en, ru, uk
- **Розмір**: 280.75 KB

### Виявлені Проблеми

#### ✅ Вирішено

- [x] Видалено 436 дублікатів ключів з en.ts
- [x] Створено єдиний централізований файл
- [x] Додано TypeScript типізацію
- [x] Налаштовано автоматичну валідацію

#### ⚠️ Потребують Уваги

- [ ] 345 відсутніх ключів (використовуються, але не визначені)
- [ ] 927 невикористаних ключів (визначені, але не використовуються)
- [ ] Hardcoded тексти у компонентах (потребує сканування)

## 💡 Використання

### Type-Safe Доступ до Перекладів

```typescript
// Імпорт типів
import type { TranslationKey } from "@/locales/types";

// Type-safe використання
const key: TranslationKey = "common.save"; // ✅ OK
const badKey: TranslationKey = "invalid.key"; // ❌ Compile error!

// У компонентах
import { useTranslation } from "next-i18n";

function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <button>{t("common.save")}</button>  // Type-checked!
  );
}
```

### Додавання Нових Перекладів

1. Відкрийте `src/locales/translations.json`
2. Додайте новий ключ у відповідну секцію:

```json
{
  "translations": {
    "myFeature": {
      "newKey": {
        "en": "New Feature",
        "ru": "Новая функция",
        "uk": "Нова функція"
      }
    }
  }
}
```

3. Регенеруйте типи:

```bash
npm run translations:generate-types
```

4. Використовуйте у коді:

```typescript
const text = t("myFeature.newKey"); // Type-safe!
```

## 🔍 Інструменти Валідації

### 1. Перевірка Покриття

```bash
npm run translations:coverage
```

**Перевіряє**:
- Чи всі використані ключі існують
- Чи є невикористані ключі
- Повноту перекладів для всіх мов

**Результат**:
- `translation-coverage-report.json` - детальний звіт
- Exit code 0/1 для CI/CD

### 2. Сканування Hardcoded Текстів

```bash
npm run translations:scan-ui
```

**Знаходить**:
- Текст між JSX тегами: `<div>Text</div>`
- Атрибути: `title="Text"`, `placeholder="Text"`
- Toast повідомлення
- Статичні рядки в const

**Результат**:
- `hardcoded-texts-report.json` - список знахідок
- Файли та рядки з проблемами

### 3. Перевірка Дублікатів

```bash
npm run translations:check
```

**Перевіряє**:
- Дублювання ключів у кожній мові
- Синхронізацію між файлами
- Кількість ключів

## 🛠️ Технічні Деталі

### Формат translations.json

```json
{
  "$schema": "./translations.schema.json",
  "meta": {
    "version": "2.0.0",
    "lastUpdated": "2025-11-07T12:00:00Z",
    "totalKeys": 1511,
    "languages": ["en", "ru", "uk"]
  },
  "translations": {
    "common": {
      "save": {
        "en": "Save",
        "ru": "Сохранить",
        "uk": "Зберегти"
      }
    }
  }
}
```

### Генерація Типів

Автоматично створюється з `translations.json`:

```typescript
export interface TranslationKeys {
  common: {
    save: string;
    cancel: string;
    // ...
  };
  // ...
}

export type TranslationKey = DeepKeys<TranslationKeys>;
```

### Алгоритм Міграції

1. Парсинг існуючих en.ts, ru.ts, uk.ts
2. Flatten всіх об'єктів
3. Видалення дублікатів (перше значення зберігається)
4. Синхронізація (додавання відсутніх ключів)
5. Сортування за алфавітом
6. Unflatten та збереження у JSON
7. Створення backup

## 📈 Roadmap

### Фаза 1: Очищення (Поточна)

- [x] Міграція у JSON формат
- [x] Видалення дублікатів
- [x] TypeScript типізація
- [ ] Додавання відсутніх 345 ключів
- [ ] Видалення 927 невикористаних ключів

### Фаза 2: Автоматизація

- [ ] Pre-commit hooks для валідації
- [ ] CI/CD інтеграція
- [ ] Автоматичне оновлення типів
- [ ] Lint правила для hardcoded текстів

### Фаза 3: Покращення

- [ ] JSON Schema валідація
- [ ] Автоматичний переклад через Google Translate API
- [ ] Візуальний редактор перекладів
- [ ] Експорт/імпорт для перекладачів

## 🔒 Best Practices

### DO ✅

1. **Використовуйте Type-Safe ключі**
   ```typescript
   const key: TranslationKey = "common.save";
   ```

2. **Запускайте валідацію перед commit**
   ```bash
   npm run translations:test
   ```

3. **Групуйте ключі логічно**
   ```json
   "auth": { "login": {...}, "logout": {...} }
   ```

4. **Додавайте контекст у назви**
   ```json
   "form.submit" замість "submit"
   ```

### DON'T ❌

1. **Не використовуйте hardcoded тексти**
   ```tsx
   ❌ <button>Save</button>
   ✅ <button>{t("common.save")}</button>
   ```

2. **Не редагуйте types.ts вручну**
   ```
   ❌ Редагування types.ts
   ✅ npm run translations:generate-types
   ```

3. **Не створюйте дублікати**
   ```json
   ❌ {"save": {...}, "buttonSave": {...}}
   ✅ {"common.save": {...}}
   ```

## 📞 Підтримка

### Часті Проблеми

#### Проблема: "Ключ не знайдено"

```bash
# Перевірте, чи ключ існує
npm run translations:coverage
```

#### Проблема: "TypeScript помилка"

```bash
# Регенеруйте типи
npm run translations:generate-types
```

#### Проблема: "Hardcoded текст"

```bash
# Знайдіть всі hardcoded тексти
npm run translations:scan-ui
```

### Контакти

- Документація: `/docs/TRANSLATIONS_SYSTEM.md`
- Issues: GitHub Issues
- Звіти: `src/locales/reports/`

---

**Версія**: 2.0.0  
**Останнє оновлення**: 2025-11-07  
**Статус**: Концепт / план розвитку (поточна реалізація описана у секції "Актуальний стан")
