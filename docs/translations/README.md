# Система перекладів

## Актуальний стан та джерело правди

Поточна реальна реалізація перекладів описана в документі [`../TRANSLATIONS_SYSTEM.md`](../TRANSLATIONS_SYSTEM.md):

- runtime-рівень побудований на `next-intl` та TS-файлах `frontend/src/locales/{en,ru,uk}.ts`;
- більшість CLI-скриптів для аналізу/виправлення (`translations:*`, `translation-*`) були винесені з `frontend/package.json` разом із dev-утилітами, залишивши тільки скрипти для деплою.

Нижче наведена розширена/концептуальна архітектура автоматизованої системи перекладів. Використовуйте її як **дорожню карту**: перед запуском будь-яких команд спочатку перевіряйте їх наявність у `package.json` та у каталозі `scripts/`.

## Огляд (концептуальна система)

Автоматизована система управління багатомовністю з інтелектуальною валідацією, аналізом коду та автоматичним виправленням неузгодженостей.

## Архітектура

### Компоненти

1. **Сканер коду** - аналіз використання ключів перекладів
2. **Аналізатор** - виявлення неузгодженостей
3. **Генератор перекладів** - створення відсутніх перекладів
4. **Система валідації** - перевірка консистентності
5. **Автоматичне виправлення** - оновлення файлів перекладів

### Підтримувані мови

- **Англійська (en)** - базова мова
- **Українська (uk)** - основна для користувачів
- **Російська (ru)** - додаткова підтримка

## Структура файлів

```
frontend/src/
├── locales/
│   ├── en.ts                  # Англійські переклади
│   ├── ru.ts                  # Російські переклади
│   ├── uk.ts                  # Українські переклади
│   └── .backups/              # Автоматичні бекапи
└── utils/dev-tools/
    ├── translation-doctor.ts        # CLI інтерфейс
    ├── translation-scanner.ts       # Сканер коду
    ├── translation-analyzer.ts      # Аналізатор
    ├── translation-generator.ts     # Генератор перекладів
    └── translation-corrector.ts     # Система виправлення
```

## Команди

### Основні команди

```bash
# Швидка перевірка стану
npm run translation-health

# Сканування коду
npm run translation-scan

# Аналіз консистентності
npm run translation-analyze

# Автоматичне виправлення
npm run translation-fix

# Інтерактивний режим
npm run translation-doctor
```

### Перевірка перекладів

```bash
# Базова перевірка
npm run check-translations

# Отримати підказки для виправлення
npm run fix-translations
```

## Функціональність

### Сканування коду

Автоматичне виявлення використання ключів перекладів:

**Підтримувані патерни**:
```typescript
// Пряме використання
t('profile.title')

// Динамічне використання
t(variable + '.title')

// Template literals
t(`profile.${section}`)

// Умовне використання
t(isAdmin ? 'admin.title' : 'user.title')
```

**Опції сканування**:
```bash
# Сканувати конкретну директорію
npm run translation-scan -- --dir src/components

# Фільтрувати результати
npm run translation-scan -- --filter "profile\."

# Зберегти результати
npm run translation-scan -- --output scan-results.json
```

### Аналіз консистентності

Виявлення проблем у перекладах:

**Типи проблем**:
- Відсутні ключі в одній з мов
- Зайві ключі що не використовуються
- Неузгодженості у структурі
- Порожні переклади
- Невалідна вкладеність

**Опції аналізу**:
```bash
# Аналіз з фільтром severity
npm run translation-analyze -- --severity error

# Використання кастомної директорії
npm run translation-analyze -- --locales src/i18n

# Експорт звіту
npm run translation-analyze -- --output analysis-report.json
```

### Автоматичне виправлення

Система автоматичного оновлення файлів перекладів:

**Можливості**:
- Створення відсутніх ключів
- Оновлення неузгоджених перекладів
- Видалення невикористовуваних ключів
- Форматування TypeScript коду
- Валідація змін

**Опції виправлення**:
```bash
# Dry run (показати що буде змінено)
npm run translation-fix -- --dry-run

# Пропустити створення бекапів
npm run translation-fix -- --no-backup

# Перезаписати існуючі переклади
npm run translation-fix -- --overwrite
```

### Генерація перекладів

Інтелектуальна система створення перекладів:

**Алгоритм генерації**:
1. Аналіз контексту використання ключа
2. Визначення домену (UI, бізнес-логіка, повідомлення)
3. Врахування автомобільної термінології
4. Адаптація для цільової аудиторії
5. Валідація згенерованого тексту

**Фактори що враховуються**:
- Структура ключа (`profile.address.city`)
- Контекст використання в коді
- Довжина тексту для UI елементів
- Граматичні особливості мови
- Культурні нюанси регіону

## Робочий процес

### Додавання нових функцій

1. **Додати англійські ключі**:
```typescript
// src/locales/en.ts
export default {
  "newFeature": {
    "title": "New Feature",
    "description": "Feature description",
    "button": "Click Me"
  }
}
```

2. **Запустити генерацію перекладів**:
```bash
npm run translation-analyze
npm run translation-fix
```

3. **Перевірити результат**:
```bash
npm run check-translations
```

4. **Використати в компонентах**:
```typescript
import { useTranslation } from '@/hooks/useTranslation';

const { t } = useTranslation();

return (
  <div>
    <h1>{t('newFeature.title')}</h1>
    <p>{t('newFeature.description')}</p>
    <button>{t('newFeature.button')}</button>
  </div>
);
```

### Pre-commit валідація

Автоматична валідація перед кожним комітом:

```bash
git add .
git commit -m "feat: add new feature"

# Автоматично запускається:
# 🔍 Перевірка перекладів...
# ✅ Валідація пройшла успішно!
```

**Якщо валідація не пройшла**:
```bash
# Детальний звіт
npm run check-translations

# Виправлення проблем
npm run translation-fix

# Повторний коміт
git add .
git commit -m "fix: resolve translation issues"
```

## Система безпеки

### Автоматичні бекапи

- Створюються перед кожною зміною
- Зберігаються з timestamp
- Автоматичне очищення (збереження останніх 10 версій)
- Можливість швидкого відновлення

**Структура бекапів**:
```
src/locales/.backups/
├── en_2024-11-05_14-30-00.ts
├── ru_2024-11-05_14-30-00.ts
└── uk_2024-11-05_14-30-00.ts
```

### Dry-run режим

Перегляд змін без застосування:
```bash
npm run translation-fix -- --dry-run

# Вивід:
# 📋 Зміни що будуть застосовані:
#   + ru.newFeature.title: "Новая функция"
#   + uk.newFeature.title: "Нова функція"
#   - en.oldKey (не використовується)
```

### Валідація

Багаторівнева система перевірки:
1. Синтаксис TypeScript
2. Структура вкладеності
3. Наявність обов'язкових полів
4. Відповідність між мовами
5. Цілісність файлів

## Інтеграція

### Git Hooks

#### Pre-commit Hook
**Файл**: `.husky/pre-commit`

```bash
#!/bin/sh
npm run check-translations
```

Блокує коміт при наявності помилок у перекладах.

#### Commit-msg Hook
**Файл**: `.husky/commit-msg`

```bash
#!/bin/sh
# Додаткова валідація для комітів з ключовими словами:
# translation, translate, locale, i18n, lang
```

### CI/CD Integration

```yaml
# GitHub Actions example
- name: Перевірка перекладів
  run: npm run check-translations
  
- name: Аналіз консистентності
  run: npm run translation-analyze --output ci-report.json
```

### IDE Integration

Експорт результатів для IDE плагінів:

```bash
npm run translation-analyze --output .vscode/translation-issues.json
```

## Звітність

### Health Score

Формула розрахунку:
```
Health Score = 100 - (Помилки × 10) - (Попередження × 5)
```

**Рівні якості**:
- 90-100: Відмінно
- 70-89: Добре
- 50-69: Задовільно
- 0-49: Потребує виправлення

### Рівні severity

- **Error**: Відсутні переклади, зламані ключі
- **Warning**: Невикористовувані переклади, неузгодженості
- **Info**: Рекомендації по стилю, оптимізація

### Формати експорту

```bash
# JSON для автоматизації
npm run translation-analyze --output report.json

# Markdown для документації
npm run translation-analyze --format markdown --output TRANSLATIONS.md

# CSV для аналізу
npm run translation-analyze --format csv --output translations.csv
```

## Best Practices

### Іменування ключів

```typescript
// ✅ Добре - ієрархічна структура
"profile": {
  "contact": {
    "addButton": "Add Contact",
    "editButton": "Edit Contact"
  }
}

// ❌ Уникати - плоска структура
"addContactBtn": "Add Contact",
"editContactBtn": "Edit Contact"
```

### Якість перекладів

- Контекстуальна відповідність
- Консистентний тон у всіх мовах
- Врахування культурних відмінностей
- Оптимальна довжина для UI

### Тестування

- Перевірка UI з різними мовами
- Тестування переповнення тексту
- Валідація спеціальних символів
- Перевірка RTL підтримки (якщо потрібно)

## Troubleshooting

### Типові проблеми

**Відсутні ключі**:
```
❌ Відсутні ключі в RU:
   - profile.contact.add
   - profile.contact.list
```

**Рішення**: Запустити `npm run translation-fix`

**Зайві ключі**:
```
⚠️ Зайві ключі в EN:
   + profile.oldFeature
```

**Рішення**: Видалити вручну або використати `--cleanup` опцію

**Проблеми з вкладеністю**:
```
❌ Неспівпадіння структури:
   EN: profile.contact.add (object)
   RU: profile.contact.add (string)
```

**Рішення**: Виправити структуру вручну у відповідному файлі

### Debug Mode

Детальне логування:
```bash
DEBUG=translation-* npm run translation-doctor
```

### Екстрені процедури

```bash
# Пропустити валідацію (тільки в екстрених випадках)
git commit --no-verify -m "emergency fix"

# Тимчасово вимкнути Husky
export HUSKY=0
git commit -m "commit without hooks"
unset HUSKY

# Відновити з бекапу
cp src/locales/.backups/en_TIMESTAMP.ts src/locales/en.ts
```

## Переваги системи

### Для розробників
- Автоматизований workflow
- Контекстуальні переклади
- Безпека через бекапи та dry-run
- Економія часу на рутинних задачах

### Для якості проекту
- Консистентність перекладів
- Повнота багатомовної підтримки
- Автоматизоване обслуговування
- Ефективна обробка великих кодових баз

### Для команди
- Стандартизація процесу перекладів
- Детальна документація та звіти
- Інтеграція з існуючими інструментами
- Простий CLI інтерфейс

---

**Остання актуалізація**: Листопад 2024  
**Статус**: Повністю функціональна  
**Покриття**: Всі TypeScript/JavaScript файли
