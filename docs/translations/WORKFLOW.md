# Робочий процес перекладів

## Швидкий довідник

### Команди

```bash
# Перевірка консистентності
npm run check-translations

# Отримати підказки для виправлення
npm run fix-translations

# Тестування pre-commit hook
git add . && git commit -m "test: translation check"
```

### Файли

- `src/locales/en.ts` - англійські переклади (базові)
- `src/locales/ru.ts` - російські переклади
- `src/locales/uk.ts` - українські переклади

## Workflow

### Додавання нових функцій

Коли додаєте нові UI елементи що потребують перекладу:

#### 1. Додати англійські ключі

```typescript
// src/locales/en.ts
export default {
  // ... існуючі ключі
  "newFeature": {
    "title": "New Feature",
    "description": "Feature description",
    "button": "Click Me"
  }
}
```

#### 2. Додати відповідні переклади

```typescript
// src/locales/ru.ts
"newFeature": {
  "title": "Новая функция",
  "description": "Описание функции",
  "button": "Нажми меня"
}

// src/locales/uk.ts
"newFeature": {
  "title": "Нова функція",
  "description": "Опис функції",
  "button": "Натисни мене"
}
```

#### 3. Використати в компонентах

```typescript
import { useTranslation } from '@/hooks/useTranslation';

const { t } = useTranslation();

return (
  <div>
    <h1>{t('newFeature.title')}</h1>
    <p>{t('newFeature.description')}</p>
    <button>{t('newFeature.button')}</button>
  </div>
);
```

### Перед комітом

Pre-commit hook автоматично запускає валідацію:

```bash
git add .
git commit -m "feat: add new feature"

# Автоматично виконується:
# 🔍 Перевірка перекладів...
# 📝 Аналіз консистентності...
# ✅ Валідація пройшла успішно!
```

### Якщо валідація не пройшла

```bash
# Вивід помилки:
# ❌ Перевірка перекладів не пройшла!
# 💡 Запустіть 'npm run check-translations' для деталей

# Отримати детальний звіт
npm run check-translations

# Отримати підказки для виправлення
npm run fix-translations
```

## Автоматизовані інструменти

### Pre-commit Hook

- **Локація**: `.husky/pre-commit`
- **Тригер**: Кожний `git commit`
- **Дія**: Валідація консистентності перекладів
- **Блокування**: Коміт не виконається при наявності помилок

### Commit Message Hook

- **Локація**: `.husky/commit-msg`
- **Тригер**: Коміти з ключовими словами перекладів
- **Ключові слова**: `translation`, `translate`, `locale`, `i18n`, `lang`
- **Дія**: Додаткова валідація для змін перекладів

### Translation Checker

- **Скрипт**: `src/utils/dev-tools/check-translations.cjs`
- **Команда**: `npm run check-translations`
- **Функції**:
  - Виявлення відсутніх ключів
  - Пошук зайвих ключів
  - Звіт про неузгодженості
  - Exit code для CI/CD інтеграції

### Translation Fixer

- **Скрипт**: `src/utils/dev-tools/fix-translations.js`
- **Команда**: `npm run fix-translations`
- **Функції**:
  - Підказки для відсутніх ключів
  - Інтелектуальні переклади
  - Готовий код для вставки

## Типові проблеми та рішення

### Відсутні ключі

```
❌ Відсутні ключі в RU:
   - profile.contact.add
   - profile.contact.list
```

**Рішення**: Додати відсутні ключі до `src/locales/ru.ts`:

```typescript
"profile": {
  "contact": {
    "add": "Добавить контакт",
    "list": "Список контактов"
  }
}
```

### Зайві ключі

```
⚠️ Зайві ключі в EN (відсутні в інших мовах):
   + profile.oldFeature
```

**Рішення**:
1. **Додати в інші мови** якщо функція все ще використовується
2. **Видалити з англійської** якщо функцію було видалено

### Неспівпадіння структури

Ключі мають зберігати однакову вкладену структуру:

```typescript
// ✅ Правильно - однакова структура
// EN
"profile": { "contact": { "add": "Add" } }
// RU
"profile": { "contact": { "add": "Добавить" } }

// ❌ Неправильно - різна структура
// EN
"profile": { "contact": { "add": "Add" } }
// RU
"profile.contact.add": "Добавить"
```

## Best Practices

### Іменування ключів

- Використовувати описові, ієрархічні ключі
- Групувати пов'язані переклади
- Використовувати camelCase для консистентності

```typescript
// ✅ Добре
"profile": {
  "contact": {
    "addButton": "Add Contact",
    "editButton": "Edit Contact"
  }
}

// ❌ Уникати
"addContactBtn": "Add Contact",
"editContactBtn": "Edit Contact"
```

### Якість перекладів

- Зберігати контекстуальну відповідність
- Підтримувати консистентний тон у всіх мовах
- Враховувати культурні відмінності

### Тестування

- Завжди тестувати UI з різними мовами
- Перевіряти переповнення тексту при довших перекладах
- Валідувати коректне відображення спеціальних символів

## Екстрені процедури

### Пропустити валідацію

```bash
# Пропустити всі hooks (тільки в екстрених випадках!)
git commit --no-verify -m "emergency fix"

# Тимчасово вимкнути Husky
export HUSKY=0
git commit -m "commit without hooks"
unset HUSKY
```

### Виправлення після екстреного коміту

```bash
# Перевірити що потребує виправлення
npm run check-translations

# Отримати підказки
npm run fix-translations

# Застосувати виправлення та закомітити
git add .
git commit -m "fix: resolve translation inconsistencies"
```

## Інтеграція з CI/CD

Перевірка перекладів в CI/CD pipeline:

```yaml
# GitHub Actions приклад
- name: Перевірка перекладів
  run: npm run check-translations
```

Скрипт завершується з exit code 1 при наявності неузгодженостей, що призводить до фейлу білду.

## Переваги

- 🛡️ **Запобігання зламаним перекладам** у продакшні
- 🔄 **Автоматична валідація** при кожному коміті
- 🌍 **Гарантована багатомовна консистентність**
- 📊 **Чіткі звіти** про проблеми
- ⚡ **Швидкий фідбек** під час розробки
- 🔧 **Корисні підказки** для розробників

---

**Остання актуалізація**: Листопад 2024  
**Статус**: Активно використовується
