# =============================================================================
# DOCKER COMPOSE INTERPOLATION - DO NOT DELETE! / ІНТЕРПОЛЯЦІЯ - НЕ ВИДАЛЯТИ!
# =============================================================================
#
# ⚠️  WARNING / УВАГА: This file is needed ONLY for Docker Compose interpolation
#     Цей файл потрібен ТІЛЬКИ для інтерполяції Docker Compose
#
# Docker Compose loads this file AUTOMATICALLY BEFORE loading env_file into containers.
# Docker Compose завантажує цей файл АВТОМАТИЧНО ПЕРЕД завантаженням env_file в контейнери.
#
# 🎯 PHILOSOPHY / ФІЛОСОФІЯ:
# - All app variables are defined in env-config/ (according to ENV_SETUP.md)
#   Всі змінні додатків визначені в env-config/ (згідно з ENV_SETUP.md)
# - This file contains ONLY critical variables for Docker Compose
#   Цей файл містить ТІЛЬКИ критично важливі змінні для Docker Compose
#
# 🚫 DO NOT ADD HERE / НЕ ДОДАВАТИ СЮДИ:
# - Application variables (they belong in env-config/)
#   Змінні додатків (вони належать в env-config/)
# - Secrets (they belong in env-config/.env.secrets)
#   Секрети (вони належать в env-config/.env.secrets)
#
# =============================================================================

# DEBUG for correct Django static files serving in Docker
# DEBUG для коректної роботи статичних файлів Django в Docker
DEBUG=true

# PostgreSQL variables needed for Docker Compose interpolation
# Змінні PostgreSQL потрібні для інтерполяції Docker Compose
POSTGRES_DB=db
POSTGRES_USER=user
